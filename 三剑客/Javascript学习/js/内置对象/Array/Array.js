/**
* 生成数组的方法
* @param {type}  
* @returns   
* */
// 1. 通过字面量的方式生成数组（推荐）
var arr = [1, 2, 3];
// 2. 通过构造函数的方式生成数组
var arr = new Array(2);

/**
* 静态方法
* @param {type}  
* @returns   
* */
// 1. Array.from() 方法从一个类似数组或可迭代对象中创建一个新的数组实例。
debugger;
